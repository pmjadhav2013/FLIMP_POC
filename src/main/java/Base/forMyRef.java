// code to check the package Base;
package Base;
import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;

public class forMyRef {
	/*
	// code to check the list elemenmt and confirm its text
	
	// get the list of the microsite names
			List<WebElement> microsite_list = driver
					.findElements(By.xpath("//div[@class='card-body']//h5"));
			System.out.println("testing"); 
			
			if (!microsite_list.isEmpty()) {
				// Hover over the first action button
				Actions act = new Actions(driver);
				act.moveToElement(microsite_list.get(0)).build().perform();
				Thread.sleep(5000);
				
				//WebElement microsite = driver.findElement(By.xpath("//div[@class = 'card-body']//h5"));
			//	String microsite_name = microsite_list.getFirst().getText();
				//-----------------
				if (!microsite_list.isEmpty() && microsite_list.get(0).getText().contains("POC_test")) {
	                System.out.println("abc is present in the first element of the list.");
	            } else {
	                System.out.println("abc is NOT present in the first element of the list.");
	            }
*/
}
